/* Podemos passar até três parâmetros pro Map

Elemento - O elemento que ele está iterando
Índice - posição do elemento 

Se vamos trabalhar com o MAP é necessário uma função para isso e o instrutor utiliza uma arrow function pra isso
*/
/*
const cursos = ['HTML', 'CSS', 'Javascript']

cursos.map((el, i)=>{
    console.log('Curso ' + el + ' posição ' + i)
})
*/
/*
PROVÁVEL RESOLUÇÃO DO PROBLEMA DO CALENDÁRIO

let el2 = ['Janeiro','Fevereiro','Março','Abril']
let mes = 2

el2.map((el,i)=>{
    if(i == mes)console.log(el)
})
*/


let numberValue = 0
console.log(numberValue++)
console.log(++numberValue)
